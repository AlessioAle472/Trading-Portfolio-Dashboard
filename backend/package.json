{
  "name": "trading-portfolio-backend",
  "version": "1.0.0",
  "description": "Express backend for Trading Portfolio Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "pip3 install -r scripts/requirements.txt || pip install -r scripts/requirements.txt || echo 'Failed to install Python dependencies'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "stripe": "^22.2.2"
  }
}
